class Todo {
  final String title;
  final String description;
  final bool isCompleted;
  final String id;

  Todo({
    required this.title,
    required this.description,
    required this.isCompleted,
    required this.id,
  });
}
